0715-0717 huge code update and bug fix.

Thus rerun the codi local_attn + full_recon experiment with the new code. The results are basically the same as before (codi_trace/results/codi_recon_localfull_1.5b_ls1_rw0.1_len128_repro).

This exp takes 1.5B SFT ck6936 as base model, and a/b/g/w weights are all 1.0.